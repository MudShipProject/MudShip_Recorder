# =====================================================================
# このリポジトリの root は UPM パッケージ本体 (com.mudship.motion-recorder)。
# 開発・検証用の Unity プロジェクト (MudShipRecorder/) は push しない。
# =====================================================================

# Unity 開発プロジェクト一式 (パッケージの一部ではない)
/MudShipRecorder/
# Unity が隣接フォルダに対して生成する meta (パッケージ外)
/MudShipRecorder.meta

# OS / エディタ生成物
.DS_Store
Thumbs.db
*.tmp
*~
